<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/head_contentLayout"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="center|center_vertical" >

    <FrameLayout
        android:id="@+id/mAnimLayout"
        android:layout_width="25dp"
        android:layout_height="60dp" >

        <ImageView
            android:id="@+id/mListHeadArrow"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_refresh_droparrow" />

        <ImageView
            android:id="@+id/mListHeadLoading"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/refresh_loading_bg"
            android:visibility="gone" />
    </FrameLayout>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_marginLeft="15dp"
        android:gravity="center_vertical" >

        <TextView
            android:id="@+id/mListStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:text="下拉刷新"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/mListUpdateTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/mListStatus"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="5dp"
            android:singleLine="true"
            android:text="最近更新：2012-04-10 22:00"
            android:textSize="12sp" />
    </RelativeLayout>

</LinearLayout>